import styled from 'styled-components';

export const AlsoOrderWrapper = styled.section`
  background-color: ${({ theme }) => theme.colors.White};
  max-width: 1170px;
  margin: 0 auto;
  padding: 0 15px;
`;

export const AlsoOrderListWrapper = styled.div`
  margin-bottom: 40px;
`;
